# Minimal mapping - only required fields
rootType: UserEvent

fields:
  eventType:
    type: string
    source: [eventType]
    required: true

  visitorId:
    type: string
    source: [visitorId]
    required: true
